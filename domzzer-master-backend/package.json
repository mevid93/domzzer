{
  "name": "domzzer-master-backend",
  "version": "1.0.0",
  "description": "Backend for the dommzer master node",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../domzzer-master-frontend/ && npm run build --prod && cp -r build ../domzzer-master-backend/"
  },
  "author": "Martin Vidjeskog",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.13"
  },
  "devDependencies": {
    "eslint": "^7.28.0",
    "nodemon": "^2.0.7"
  }
}
